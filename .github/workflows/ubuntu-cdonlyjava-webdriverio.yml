# WebdriverIO + sealights-webdriverio-plugin against the Spring Boot app instrumented with the SeaLights CD Java agent.
#
# Prerequisites (same as ci.yml / Playwright flow):
#   Repository secrets: SL_TOKEN, SL_LAB_ID (Settings → Secrets and variables → Actions)
#   Optional: SL_PROXY
#   Test runners do not pass BuildSessionId; correlation uses token + lab + test stage (per org policy).
#
# SeaLights:
#   - JVM: CD agent (javaagent) — backend coverage while the app serves /api/calculate
#   - Node: WebDriverIO plugin — test stage, start/end test, browser agent coordination
#
# Docs:
#   CD Java: https://docs.sealights.io/knowledgebase/setup-and-configuration/sealights-agents-and-plugins/cd-agent/cd-agent-for-java-application
#   WDIO plugin: https://www.npmjs.com/package/sealights-webdriverio-plugin
name: CI WebdriverIO SeaLights

on:
  workflow_dispatch:

jobs:
  webdriverio:
    runs-on: ubuntu-latest
    timeout-minutes: 25

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Create SeaLights token file
        run: |
          echo -n "${{ secrets.SL_TOKEN }}" > sltoken.txt
          chmod 600 sltoken.txt

      - name: Verify Actions secrets are available
        env:
          SL_LAB_ID: ${{ secrets.SL_LAB_ID }}
        run: |
          if [ ! -s sltoken.txt ]; then
            echo "::error::SL_TOKEN is empty or not set."
            exit 1
          fi
          if [ -z "$SL_LAB_ID" ]; then
            echo "::error::SL_LAB_ID is empty or not set."
            exit 1
          fi

      - name: Download SeaLights CD Agent
        run: |
          curl -fsSL -o sl-cd-agent.zip "https://agents.sealights.co/sl-cd-agent/sl-cd-agent-latest.zip"
          unzip -q sl-cd-agent.zip -d sealights-cd-agent
          test -f sealights-cd-agent/sl-cd-agent.jar

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: maven

      - name: Build application
        run: mvn -B clean package -DskipTests

      - name: Start calculator app (SeaLights CD javaagent)
        env:
          SL_LAB_ID: ${{ secrets.SL_LAB_ID }}
          SL_PROXY: ${{ secrets.SL_PROXY }}
        run: |
          set -e
          JAR=$(ls target/calculator-demo-*.jar | head -1)
          AGENT="${GITHUB_WORKSPACE}/sealights-cd-agent/sl-cd-agent.jar"
          TOKEN_FILE="${GITHUB_WORKSPACE}/sltoken.txt"
          JAVA_OPTS=(
            "-javaagent:${AGENT}"
            "-Dsl.tokenFile=${TOKEN_FILE}"
            "-Dsl.appName=calculator-demo"
            "-Dsl.labId=${SL_LAB_ID}"
            "-Dsl.buildname=${{ github.run_number }}"
            "-Dsl.branchname=ubuntu-wdio"
            "-Dsl.includes=com.example.*"
            "-Dsl.log.toConsole=true"
            "-Dsl.log.level=info"
            "--add-opens=java.base/jdk.internal.loader=ALL-UNNAMED"
          )
          if [ -n "$SL_PROXY" ]; then
            JAVA_OPTS+=("-Dsl.proxy=${SL_PROXY}")
          fi
          java "${JAVA_OPTS[@]}" -jar "$JAR" &
          echo "APP_PID=$!" >> "$GITHUB_ENV"
          for i in $(seq 1 90); do
            if curl -fsS http://127.0.0.1:8080/ >/dev/null 2>&1; then
              exit 0
            fi
            sleep 1
          done
          echo "App did not become ready in time"
          exit 1

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: npm
          cache-dependency-path: webdriverio/package-lock.json

      - name: Install WebdriverIO dependencies
        working-directory: webdriverio
        run: |
          if [ -f package-lock.json ]; then npm ci; else npm install; fi

      - name: Install Google Chrome (Chromedriver service needs a Chrome binary)
        run: |
          set -e
          if command -v google-chrome-stable >/dev/null 2>&1; then
            google-chrome-stable --version
            exit 0
          fi
          wget -q https://dl.google.com/linux/direct/google-chrome-stable_current_amd64.deb
          sudo apt-get update
          sudo apt-get install -y ./google-chrome-stable_current_amd64.deb

      - name: Run WebdriverIO tests (sealights-webdriverio-plugin)
        working-directory: webdriverio
        env:
          BASE_URL: http://127.0.0.1:8080
          CI: true
          SL_TOKENFILE: ${{ github.workspace }}/sltoken.txt
          SL_TESTSTAGE: wdio
          SL_TESTPROJECTID: sample-java-wdio
          SL_LABID: ${{ secrets.SL_LAB_ID }}
          SL_PROXY: ${{ secrets.SL_PROXY }}
          NODE_DEBUG: ''
        run: npx wdio run wdio.conf.mjs

      - name: Stop app
        if: always()
        run: kill "$APP_PID" 2>/dev/null || true
